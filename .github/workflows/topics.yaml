name: "Deploy topic"
on:
  push:
    paths:
      - nais/topics/**
      - .github/workflows/topics.yaml
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [ opened, synchronize, reopened, ready_for_review ]
  merge_group:
    types: [ checks_requested ]
  workflow_dispatch:
concurrency:
  group: deploy-topics-${{ github.ref }}
  cancel-in-progress: true
env:
  TOPIC_DEV_FILES: nais/topics/syfo-narmesteleder-leesah-dev.yaml
  TOPIC_PROD_FILES: nais/topics/syfo-narmesteleder-leesah-prod.yaml
jobs:
  deploy-topic-to-dev:
    name: Deploy topic to dev
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Deploy topics to dev
        uses: nais/deploy/actions/deploy@v2 # internal Nav action
        env:
          VAR: kafka-pool=nav-dev
          CLUSTER: dev-gcp
          RESOURCE: ${{ env.TOPIC_DEV_FILES }}

  deploy-topic-to-prod:
    if: github.ref == 'refs/heads/main'
    name: Deploy topic to prod
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Deploy topics to prod
        uses: nais/deploy/actions/deploy@v2 # internal Nav action
        env:
          VAR: kafka-pool=nav-prod
          CLUSTER: prod-gcp
          RESOURCE: ${{ env.TOPIC_PROD_FILES }}
